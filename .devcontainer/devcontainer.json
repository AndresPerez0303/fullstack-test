{
  "name": "Fullstack Live Coding",
  "image": "mcr.microsoft.com/devcontainers/java:21",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "forwardPorts": [8080, 3000],
  "postCreateCommand": "cd ${containerWorkspaceFolder}/app_react && yarn install --immutable && cd ${containerWorkspaceFolder}/app_spring && chmod +x ./gradlew && ./gradlew build",
  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "vscjava.extension-pack-for-java"
      ]
    }
  }
}